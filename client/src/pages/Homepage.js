import AlbumCard from "../components/AlbumCard"

const Homepage = (props) => {
    return(
        <div>
            <AlbumCard/>
            <h1>HOMEPAGE</h1>
        </div>
    )
};

export default Homepage;